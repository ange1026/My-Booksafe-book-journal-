{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/books" class="navbar-brand">My Booksafe</a>
        {% if loggedIn %}
        <a href="/books" class="nav-link text-light">Hello {{ username }} </a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
                {%- comment -%} <a href="/books" class="nav-link text-light">View All</a> {%- endcomment -%}
                <a href="/books/new" class="nav-link text-light">New Book</a>
                <a href="/books/mine" class="nav-link text-light">My Books</a>
                <a href="/users/logout" class="nav-link text-light">Log Out</a>
            {% else %}
                <a href="/users/signup" class="nav-link text-light">Sign Up</a>
                <a href="/users/login" class="nav-link text-light">Sign In</a>
            {% endif %}
        </div>
    </div>
</nav>
{% endblock %}